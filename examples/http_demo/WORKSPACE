workspace(name = "http_demo")

# Consume the cpp_network library as an external Bazel workspace via
# local_repository, demonstrating real network requests with netlib.
local_repository(
    name = "cpp_network",
    path = "../..",
)
